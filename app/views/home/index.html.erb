<!DOCTYPE html>
<html>
    <head>
        <title>About Me</title>
        <meta name="viewport" content="width=device-width">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
        <link rel="stylesheet" href="/portfolio.css" type="text/css" />
    </head>
    <body>
        <div class="jumbotron">
            <div class="row">
                <div class="col-md-3">
                    <img class="img" src="https://www.w3schools.com/w3css/img_lights.jpg" width="300px"></img>
                </div>    
                <div class="col-md-9">
                    <h1 class="display-4">Hello! This is a page about me.</h1>
                </div>     
            </div>
            <hr class="my-4">
            <a class="btn btn-info btn-lg" href="#" role="button">Learn more</a>
        </div>
        <div class="container">
            <div class="row">
                <div class="col-md-16">
                   <h2 class="title-2">Hobbies</h2> 
                </div>
            </div>    
            <div class="row">
                <div class="col-md-4">
                    <h2>UX</h2>
                </div>  
                <div class="col-md-4">
                    <h2>Product Management</h2>
                </div>
                <div class="col-md-4">
                    <h2>Optimization</h2>
                </div>
            </div>
        </div>
        <div class="card" style="width: full;">
            <div class="card-body">
            <h5 class="card-title">Extras</h5>    
                <p class="card-text">Want to know anything else?</p>
            <a href="#" class="btn btn-info">Contact Me</a>    
            </div>
        </div>
        <script>
            var title = $('.display-4');
            title.text("Welcome to my blog!");
            title.css({color: "green", fontSize: "40px"});
            
            function getRandomColor(){
            return "#000000".replace(/0/g,function(){return (~~(Math.random()*16)).toString(16);});
            }
            
            var randomColor = getRandomColor();
            
            title.on('click', changeTitleColor);
            function changeTitleColor(){
            title.css({color: randomColor});
            }
            
            const productSchoolLat = "37.796465";
            const productSchoolLng = "-122.402492";
            var weatherDescription = $('.title-2');

            //$.ajax({
                 //url: 'api/v1/weather',
                 //type: 'GET',
                 //data: { lat: response, lng: productSchoolLng } ,
                 //contentType: 'application/json; charset=utf-8',
                 //success: function (response) {
                    //weatherDescription.text(response.currently.summary);
                    //console.log(response);
                    //},
                //error: function (error) {
                    //console.log(error);
                    // }
            //});
                
            navigator.geolocation.getCurrentPosition((location) => {
            var longitude = location.coords.longitude;
            var latitude = location.coords.latitude;
            
            $.ajax({
                url: 'api/v1/weather',
                type: 'GET',
                data: { lat: latitude, lng: longitude } ,
                contentType: 'application/json; charset=utf-8',
                success: function (response) {
                    weatherDescription.text(response.currently.summary);
                    console.log(response);
                    }
            });
            });
            
        </script>    
    </body>
</html> 